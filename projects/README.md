# Projects

Each team project belongs in its own folder under this directory.

Copy `_template` to start:

```text
cp -R _template your-project-name
```

Use a short lowercase folder name with hyphens. The project README is the important part; do not worry about making the codebase perfect during the sprint.
